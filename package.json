{
  "name": "rojoiner-web",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Open-source Roblox joining toolkit with player lookup, public server browsing, relationship checks, and an optional friends companion.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/xtofuub/RoJoiner.git"
  },
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check api/search.js && node --check api/game.js && node --check api/compare.js && node --check lib/roblox.js && node --check public/app.js && node --check public/share.js && node --check public/app-wrapper.js && node --check companion/popup.js"
  },
  "engines": {
    "node": ">=20"
  }
}
