# Downloads

Binary builds in this directory are generated from the open-source files in `companion/`.

`rojoiner-companion-firefox-0.2.0.zip` is an unsigned Firefox test build intended for loading through `about:debugging`. Standard Firefox requires Mozilla signing for permanent installation.
